import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  // Primary Colors (Eco-Green)
  static const Color primary = Color(0xFF0B6947);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color primaryContainer = Color(0xFF30835F);
  static const Color onPrimaryContainer = Color(0xFFF5FFF6);

  // Secondary Colors (Volt-Blue)
  static const Color secondary = Color(0xFF00677D);
  static const Color onSecondary = Color(0xFFFFFFFF);
  
  // Background & Surface
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color onSurface = Color(0xFF191C1D);
  static const Color onBackground = Color(0xFF191C1D);

  // Text Colors
  static const Color textPrimary = Color(0xFF191C1D);
  static const Color textSecondary = Color(0xFF3F4943);
}
